#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);
    
    int n, t;       // the number of books and the number of free time
    cin >> n >> t;

    // the vector contains minutes to read books
    int minutes[n];   
    for (int i = 0; i < n; i++){
        cin >> minutes[i];
    }   

    int max;
    for (int i = 0; i < n; i++){
        int sum = 0;
        for (int j = 0; j < n; j++){
            if ((t - sum) > a[j+1]
        }

    }

    cout << max << endl;

    return 0;
}
