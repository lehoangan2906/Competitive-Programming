#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    int n;  // the number of books in the library
    cin >> n;

    int arr[n]; // times to read each book
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    
 
    return 0;
}